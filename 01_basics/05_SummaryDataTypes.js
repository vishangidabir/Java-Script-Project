// Primitives

// 7 types : String, Number, Boolean, null, undifined, symbol, BigInt

// Non-primitives (Reference)
// Array, objects,Functions


// *********************Memories****************************

// stack(Primities), Heap(non-primitives)

let myName = "Vioshanbvchb"

let anotername = myName

let userOne = {
    email: "user@gmail.com",
    upiId: "user@ybl"
}